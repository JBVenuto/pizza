//Require the express package
var express = require("express");
//Import model burger.js file
var burger = require("../models/pizza.js");

//Router to direct the app
router = express.Router();

//Create the routes 

//Export the router
module.exports = router;